# Опросник ДНП "Клеймёново-2"

Веб-приложение для сбора и анализа мнений жителей ДНП "Клеймёново-2" по вопросам создания нового СНТ.

## Технологии
- FastAPI (backend)
- Jinja2 (шаблоны)
- SQLite (база данных)
- Pandas + Bokeh (статистика и графики)

## Основной функционал
- Двухэтапный опрос: базовая и расширенная часть
- Проверка уникальности по email, телефону, кадастровому номеру
- Верификация через email (код подтверждения)
- Кэширование статистики и графиков (TTL 3 минуты)
- Актуализация статистики при каждом новом ответе с кадастровым номером
- Вся статистика и аналитика строится через pandas

## Последние изменения
- **Фильтрация по кадастровому номеру:**
  - В пирогах и статистике первой части учитываются только анкеты с кадастровым номером.
  - Без кадастрового номера учитываются только ответы на второй странице (детальная аналитика).
- **Три типа анкет:**
  1. Полные анкеты с кадастровым номером (обе части)
  2. Частичные анкеты с кадастровым номером (только первая часть)
  3. Анкеты без кадастрового номера (только вторая часть)
- **Блок "Общая информация"** теперь показывает количество анкет каждого типа.
- **Сброс кэша статистики** теперь происходит не только после завершения опроса, но и сразу после отправки первой части с кадастровым номером.
- **UX:**
  - Если выбран "Нет кадастрового номера", вопросы про СНТ и взносы скрываются.
  - Добавлены пояснения для пользователя о том, как учитываются ответы.
  - Вариант "Оставить всё как есть" отображается в пироге как отдельный сектор.
- **Оптимизация:**
  - SQL-запросы ускорены, добавлены индексы.
  - Вся логика фильтрации реализована на pandas.

## Как запустить
1. Установить зависимости:
   ```
   pip install -r requirements.txt
   ```
2. Запустить сервер:
   ```
   uvicorn app.main:app --reload
   ```
3. Перейти на http://localhost:8000

## Структура кода
- `app/api/` — роуты FastAPI
- `templates/` — Jinja2-шаблоны (index, stats, details)
- `app/models.py` — SQLAlchemy-модели
- `app/crud.py` — работа с БД
- `app/api/stats.py` — статистика, графики, кэш

## Важно
- Все новые ответы с кадастровым номером сразу попадают в статистику.
- Без кадастрового номера — только аналитика второй части.
- Кэш статистики сбрасывается автоматически после каждого релевантного действия.

## ⚡ Кэширование и актуализация статистики

- Вся статистика, графики и комментарии кэшируются в памяти сервера (in-memory cache) для ускорения работы.
- Кэш автоматически обновляется (TTL) раз в 3 минуты.
- Кэш сбрасывается мгновенно:
  - После отправки первой части анкеты с кадастровым номером (базовые вопросы)
  - После отправки второй части анкеты (детальные вопросы)
- При сбросе кэша пересчитываются:
  - Статистика по анкетам
  - Все графики (Bokeh)
  - Комментарии для дашборда
  - Все комментарии для отдельной страницы
- Это гарантирует, что новые ответы сразу попадают в аналитику и графики, а нагрузка на сервер минимальна.

---

Если что-то не работает — см. комментарии в коде или пиши в issues.

## 📋 Описание проекта

**Контекст:** ДНП "Клеймёново-2" (ОГРН 1085043000624) подлежит исключению из ЕГРЮЛ по решению налогового органа №6336 от 07.04.2025. Инициативная группа жителей проводит опрос для выяснения мнения о создании нового СНТ.

**Цель:** Сбор структурированных данных о позиции жителей и анализ готовности к созданию нового садоводческого некоммерческого товарищества.

## 🚀 Основной функционал

### 🎯 Система опросов
- **Двухэтапное заполнение:** базовая информация + детальное мнение
- **Email верификация:** подтверждение через код (валиден 10 минут)
- **Защита от дублирования:** проверка по email, телефону, кадастру
- **Валидация данных:** комплексная проверка на клиенте и сервере
- **Прогресс-бар:** визуальное отслеживание этапов заполнения

### 🔐 Система безопасности
- **Согласие на обработку ПД:** обязательное согласие по ФЗ-152
- **AI-модерация комментариев:** фильтрация через DeepSeek API
- **Мягкая модерация:** сохранение всех данных, скрытие неподходящих
- **Временные коды:** автоматическое истечение верификационных кодов

### 💬 Система комментариев
- **Модерированные комментарии:** только прошедшие AI-проверку
- **Система лайков:** возможность оценить комментарии других
- **Карусель комментариев:** красивое отображение на странице статистики
- **Защита от спама:** минимум 10 символов, осмысленные предложения

### 📊 Аналитика и визуализация
- **Интерактивные графики:** Bokeh charts с анимацией
- **Responsive дизайн:** адаптация под мобильные устройства
- **Детальная статистика:**
  - Поддержка создания СНТ (круговая диаграмма)
  - Готовность к финансовым обязательствам
  - Основные опасения жителей (горизонтальная диаграмма)
  - Приемлемые размеры взносов
  - Готовность к участию в управлении
  - Приоритеты по важности (ранжированная диаграмма)

### 🎨 Пользовательский интерфейс
- **Современный дизайн:** градиенты, тени, анимации
- **Цветовая схема:** тематические блоки (красный, жёлтый, синий, зелёный, фиолетовый)
- **Умные подсказки:** tooltip'ы при ошибках валидации
- **Автоматические переходы:** тайм-аут на странице "Спасибо" (20 сек → статистика)
- **Навигация:** кнопки возврата, breadcrumbs

## 🛠️ Техническая архитектура

### Backend
- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с базой данных  
- **SQLite** - локальная база данных (`data/db.sqlite3`)
- **Pydantic** - валидация данных
- **DeepSeek API** - AI-модерация комментариев

### Frontend
- **Jinja2** - шаблонизатор
- **Vanilla JavaScript** - интерактивность
- **CSS3** - стилизация с градиентами и анимациями
- **Responsive design** - адаптивность

### Аналитика
- **Pandas** - обработка данных
- **Bokeh** - интерактивные графики
- **Dynamic positioning** - умное позиционирование элементов

### DevOps
- **Docker** - контейнеризация
- **Railway** - платформа развёртывания
- **Git** - система контроля версий

## 📁 Структура проекта

```
📦 Клеймёного-2/
├── 🗃️ app/                     # Основное приложение
│   ├── 🔌 api/                 # API endpoints
│   │   ├── survey.py           # Опросы и сохранение данных
│   │   ├── stats.py            # Статистика и графики
│   │   └── questions.py        # Управление вопросами
│   ├── 🤖 comment_filter.py    # AI-модерация комментариев
│   ├── 📧 email_service.py     # Отправка email кодов
│   ├── 🗄️ models.py           # Модели базы данных
│   ├── 🔧 crud.py              # Операции с данными
│   ├── ⚙️ config.py            # Конфигурация
│   └── 🚀 main.py              # Точка входа приложения
├── 🎨 templates/               # HTML шаблоны
│   ├── index.html              # Главная страница + базовый опрос
│   ├── survey-details.html     # Детальный опрос
│   ├── thanks.html             # Страница благодарности
│   ├── stats.html              # Аналитика и графики
│   ├── comments.html           # Просмотр комментариев
│   └── consent.html            # Согласие на обработку ПД
├── 📄 static/                  # Статические файлы
│   ├── consent.js              # Логика модального окна
│   └── docs/                   # Документы
│       └── ЕГРЮ_выписка.pdf    # Официальная выписка
├── 🗃️ data/                    # База данных
│   └── db.sqlite3              # SQLite база
├── 📋 requirements.txt         # Python зависимости
├── 🐳 Dockerfile              # Конфигурация Docker
├── ⚙️ railway.toml            # Конфигурация Railway
└── 📖 README.md               # Документация (этот файл)
```

## 🗄️ Схема базы данных

### Таблицы:
- **`questions`** - вопросы опроса
- **`responses`** - сессии пользователей
- **`answers`** - ответы пользователей (с полем `moderated`)
- **`verification_codes`** - коды подтверждения email
- **`comments_likes`** - лайки к комментариям

## 🔧 Установка и запуск

### Локальная разработка

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd Клеймёного-2
```

2. **Создание виртуального окружения:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. **Установка зависимостей:**
```bash
pip install -r requirements.txt
```

4. **Настройка переменных окружения:**
Создайте файл `.env`:
```env
DEEPSEEK_API_KEY=sk-ваш-ключ-deepseek
EMAIL_USER=ваш-email@gmail.com
EMAIL_PASSWORD=ваш-app-пароль
```

5. **Инициализация базы данных:**
```bash
python init_db.py
python add_questions.py
```

6. **Запуск сервера:**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Развёртывание через Docker

1. **Сборка образа:**
```bash
docker build -t kleimenovo-survey .
```

2. **Запуск контейнера:**
```bash
docker run -p 8000:8000 kleimenovo-survey
```

## 🌐 Основные страницы

- **`/`** - Главная страница с информацией и базовым опросом
- **`/survey-details`** - Детальный опрос (доступен после заполнения базового)
- **`/thanks`** - Страница благодарности (с автопереходом на статистику)
- **`/stats`** - Аналитика и графики
- **`/comments`** - Просмотр всех комментариев
- **`/consent`** - Страница согласия на обработку ПД

## 🎯 Ключевые особенности

### AI-модерация комментариев
- Интеграция с DeepSeek API для анализа русского текста
- Фильтрация мата, агрессии и спама
- Сохранение всех комментариев в БД с пометкой `moderated`
- Fallback система при недоступности AI

### Умная типографика
- Правильный перенос предлогов в многословных кнопках
- Responsive дизайн для мобильных устройств
- Цветовое кодирование разделов

### Безопасность данных
- Валидация на всех уровнях (клиент + сервер + БД)
- Временные коды верификации
- Защита от повторных отправок
- Согласие на обработку ПД

## 📈 Статистика проекта

- **6 HTML страниц** с адаптивным дизайном
- **18 вопросов** в опросе (базовые + детальные)
- **5+ типов графиков** Bokeh с интерактивностью
- **4 API модуля** для разных функций
- **AI-модерация** на русском языке
- **Полная мобильная адаптация**

## 👥 Для жителей ДНП "Клеймёново-2"

Этот опрос поможет инициативной группе понять мнение жителей и принять обоснованное решение о создании нового СНТ. Ваши данные обрабатываются конфиденциально в соответствии с ФЗ-152.

**Результаты опроса** будут представлены на общем собрании жителей.

---

**Разработано:** 2025  
**Технологии:** FastAPI, SQLite, Bokeh, DeepSeek AI  
**Статус:** ✅ Готово к эксплуатации 